import XCTest
@testable import AiryTextfield

final class AiryTextfieldTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AiryTextfield().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
